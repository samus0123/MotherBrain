__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
.venv/

# Weights are produced by `motherbrain init` / `motherbrain train`, not
# committed: they are large, they are binary, and `init` regenerates
# identical weights from a seed anyway.
checkpoints/
*.weights.h5
